enum AppStatus {
  initial,
  loading,
  success,
  complete,
  failure,
  thirdPartyFailure,
  resend,
  authenticated,
  logout,
  confirmation,
}

enum ButtonType { normal, icon, outline, text }

enum Flavor { prod, dev }
